# Manual Test Folder

This folder is used to test the plugin locally.

## Usage

1. Build the plugin: `bun run build`
2. Copy the built plugin to `.opencode/plugin/` in this folder
3. Run opencode in this directory to test the plugin functionality

## Contents

- `.opencode/plugin/` - Contains the built plugin for local testing
- `docs/` - Test documentation files for plugin testing
