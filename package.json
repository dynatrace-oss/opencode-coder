{
  "name": "@hk9890/opencode-coder",
  "version": "0.1.0",
  "description": "OpenCode plugin for story-driven development with agents and commands",
  "author": "hk9890",
  "repository": {
    "type": "git",
    "url": "https://github.com/hk9890/opencode-coder.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "type": "module",
  "main": "dist/opencode-coder.js",
  "types": "dist/opencode-coder.d.ts",
  "exports": {
    ".": {
      "types": "./dist/opencode-coder.d.ts",
      "import": "./dist/opencode-coder.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/opencode-coder.js --target bun",
    "dev": "bun --watch src/index.ts",
    "test": "bun test",
    "test:unit": "bun test tests/unit",
    "test:integration": "bun test tests/integration",
    "test:e2e": "bun test tests/e2e --timeout 60000",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "clean": "rm -rf dist",
    "typecheck": "bun x tsc --noEmit",
    "mtest": "bun run build && cd manual-test && OPENCODE_DISABLE_DEFAULT_PLUGINS=true opencode --print-logs --log-level DEBUG",
    "qtest": "bun run build && cd manual-test && OPENCODE_DISABLE_DEFAULT_PLUGINS=true opencode --print-logs run hallo"
  },
  "keywords": [
    "opencode",
    "plugin",
    "ai",
    "coding-assistant"
  ],
  "license": "MIT",
  "dependencies": {
    "@opencode-ai/plugin": "^1.0.185",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.7.2"
  }
}
