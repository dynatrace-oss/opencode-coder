---
description: Story cleanup
---

# Story cleanup

Your task is to move completed stories from `docs/stories` to `docs/archive/stories`.

## Tasks

1. Read `docs/kb/story.md` to understand what stories are and how to work with them.
2. Read the frontmatter section in all files in `docs/stories`, list all the stories sort them by number, print current
   state
3. Move all stories to `docs/archive/stories` that are in state `Done` but only leaf the last 2. So after this operation
   the older should only have 2 done stories all the others should have any other state
4. Summarize what you have done for the user (in a compact from)
5. Commit all the changes 

