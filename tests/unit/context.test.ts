import { describe, it } from "bun:test";

describe("BeadsContext (removed)", () => {
  it("guidance is provided by agent configurations, not injected programmatically", () => {
    // BeadsContext, CommandExecutor, BEADS_GUIDANCE, and bd prime integration
    // have been removed. All beads guidance and CLI reference is provided
    // by the agent configurations (ai-resources/agents/*.md).
    //
    // See epic oc-m1sq for details.
  });
});
